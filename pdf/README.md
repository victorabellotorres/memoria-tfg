# PDF generado

Al ejecutar `../scripts/build-pdf.sh`, el documento final se guarda aquí como
`memoria-tfg.pdf`. Los PDF generados están ignorados por Git; esta carpeta solo
versiona estas instrucciones.
